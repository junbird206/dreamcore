# Dreamcore — 작업 현황

> 이 파일은 **항상 덮어쓴다.** 이력은 git에 있으니 여기에 로그를 쌓지 말 것.
> 작업을 시작하기 전에 읽고, 의미 있는 작업 단위가 끝나면 갱신한다.

- **마지막 갱신:** 2026-09-01 / Claude
- **브랜치:** main → `github.com/junbird206/dreamcore` (public)
- **배포:** https://dreamcore.junbird521.workers.dev (Cloudflare Workers)

## 지금 상태
**배포 완료. 라이브에서 해몽·이미지·GA·쿠키 전부 동작 확인.**
https://dreamcore.junbird521.workers.dev

## 방금 한 일
- **이미지 한도 상향**: 브라우저 4 → 10회/일, IP 40 → 150회/일.
  해몽과 이미지를 함께 생성하므로 이미지 한도가 곧 사용 가능 횟수다.
  4회는 꿈 네 개면 소진돼 정상 사용자를 막았다. IP 천장도 함께 올렸다 —
  CGNAT으로 IP를 공유하는 모바일 사용자가 서로의 몫을 깎아먹기 때문이다
- 최악의 경우 IP 하나가 하루 약 7,000원. 월 지출 한도(₩50,000)가 최종 방어선이다
  흐름이 모순이었다. 2단계 로딩(해몽 먼저, 이미지 뒤이어)은 유지
- **화풍을 진짜 선택 사항으로.** 기본 선택을 없애고(`DreamStyle | null`), 고르지 않으면
  Gemini가 꿈에 어울리는 화풍을 직접 정한다. 같은 칩을 다시 누르면 선택 해제된다
- 라벨에 `(선택)`, 레일 아래에 "화풍을 선택하지 않아도 꿈 이미지는 정상적으로 생성됩니다"
- 이미지 대기 문구를 "꿈 속 모습을 구현하는 중…"으로 바꾸고, 그 아래
  **"대학생이라면 AI 1년 혜택 받기"** 프로모션 링크를 넣었다(goo.gle/koreaseoul).
  기다리는 몇 초가 사용자의 주의가 비는 유일한 구간이다
- 이미지 실패 시 서버가 준 사유를 그대로 보여준다 — 한도 초과와 일반 실패는
  사용자에게 다르게 읽혀야 한다
  화풍은 "어떻게 그릴지"일 뿐 "무엇을 그릴지"가 아니며, 화풍 때문에 꿈에 없던 소재를
  넣지 말라고 명시했다. 바닷가 꿈 + 백룸 조합으로 검증 — 바닷가가 유지된 채
  백룸 특유의 형광등·질감으로만 렌더된다
- **이미지 생성을 별도 버튼으로 분리.** 해몽을 먼저 보여주고 `꿈 속 이미지 생성하기`를
  눌러야 생성한다. 남용과 불필요한 비용을 막는 장치다
- 이미지가 분리됐으므로 메인 CTA를 "내 꿈 해몽 듣기"로 변경
- 결과 순서를 제목 → 해몽 → 이미지 → 공유로 정리 (읽고 나서 이미지를 결정하게)
  인식하는 구조라 네트워크 오류가 잦았다. 사용자 판단으로 걷어냈다
- **예시 꿈 문장을 기본값에서 제거.** `useState(starterDream)`이라 입력창에 실제 값으로
  들어가 있어 지우고 써야 했다. 빈 칸에서 시작하고 placeholder만 안내한다
- CTA는 20자 미만이면 비활성 — 빈 칸에서 시작하므로 첫 진입 시 비활성이 정상이다
  2단 그리드(`hero-grid`)를 없애고 `max-w-[560px]` 중앙 정렬 한 줄로 바꿨다
- 화풍 12개를 **가로 스크롤 레일**로 (`scroll-snap`). 다 펼치면 화면을 차지한다.
  약 3.6개가 보여 더 있다는 게 드러나고, 페이지 자체에는 가로 스크롤이 생기지 않는다
- CTA 문구를 "내 꿈속 장면 생성하기 & 해몽 듣기"로 바꾸고 전체 폭으로
- **개발 중 문구 제거**: "1단계 Mock 모드 / API 키 없이 전체 흐름을 먼저 검증합니다"가
  방문자에게 그대로 노출되고 있었다. 이미 실제 API가 붙은 상태였다
- **내부 KPI 제거**: 히어로의 10K 방문 목표 / 100 sign-up 목표 / 1% 전환 기준.
  방문자에게 보여줄 값이 아니다
- 테스트에 두 항목이 다시 새어나오지 않는지 역검증을 넣었다
  뽑았더니 구도가 겹쳐 구분이 안 됐다 — 백룸은 누런 사무실, 페어리코어는 버섯 숲,
  우키요에는 파도, 베이퍼웨이브는 90년대 상가 식으로 그 미학의 대표 소재를 썼다
- 카드 높이를 75%로 줄였다 (151x75 → 151x57, 비율 2:1 → 8:3)
- 썸네일 URL에 `?v=THUMB_VERSION` 쿼리를 붙였다. 파일명을 유지한 채 내용만 바꾸면
  재방문자가 옛 이미지를 계속 본다 — 교체할 때마다 `DreamLab.tsx`의 이 숫자를 올릴 것
  생성한 썸네일을 배경으로 쓴다 — 말로 설명하는 것보다 결과를 보여주는 편이 고르기 쉽다
- 칩 높이를 낮췄다(5:2 비율). 선택 영역이 3줄로 줄어 폼 전체가 짧아졌다
- 썸네일 12장은 이미 뽑아둔 비교 샘플을 재활용했다(추가 생성 비용 0원). 총 204KB
- `hint`는 화면에서만 빼고 데이터로는 유지 — Gemini 프롬프트와 `aria-label`에 쓰인다
  위어드코어 / 아날로그 호러 / 드림코어 / 초현실주의 / 형이상학 회화 / 페어리코어 /
  코티지코어 / 인상주의 / 우키요에 / 베이퍼웨이브
- 12종 전부 같은 꿈으로 실제 생성해 서로 구분되는지 확인함
- 화풍은 **이미지 프롬프트에만** 반영되고 해몽 문장에는 섞이지 않는다
  (화풍은 사용자가 고른 표현 방식이지 꿈에서 본 것이 아니다)
  SVG는 인스타·카톡이 못 받아 공유 경로가 막혀 있었다. 2,088KB → 239KB
- 공유하기가 `canShare`로 파일 지원을 확인해 이미지를 첨부한다(모바일에서 인스타로 바로)
- **음성 입력 추가** (`app/lib/speech.ts`, Web Speech API, `ko-KR`).
  미지원 브라우저(Firefox)에서는 버튼이 숨는다. 침묵으로 끊기면 자동 재시작
